package work.fking.pangya.game.packet.handler;

import io.netty.channel.Channel;
import work.fking.pangya.game.packet.inbound.Unknown156Packet;
import work.fking.pangya.networking.protocol.InboundPacketHandler;

public class Unknown156PacketHandler implements InboundPacketHandler<Unknown156Packet> {

    @Override
    public void handle(Channel channel, Unknown156Packet packet) {

    }
}
